# Example environment variables for Knex (PostgreSQL)
# Either set DATABASE_URL or the individual values below
# DATABASE_URL=postgres://user:password@localhost:5432/mydatabase
HOST=localhost
PORT=5432
USER=postgres
PASSWORD=postgres
DATABASE=mydatabase

# Optional: set NODE_ENV to development to use development config
# NODE_ENV=development

